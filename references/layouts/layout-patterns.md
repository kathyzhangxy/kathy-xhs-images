# 布局模式参考

> 所属 Skill：kathy-xhs-images
> 更新日期：2026-04-05
>
> **重要**：以下模板为风格无关的布局结构。编写 Prompt 时，需注入当前风格的色彩、线条、点缀约束（见 `references/styles/`）。

## 布局 A：浏览器预览 + 功能清单

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
├─────────────────────────────────┤
│  ┌─────────────────┐  ☑ 功能1    │
│  │  浏览器预览窗口  │  ☑ 功能2    │
│  │                 │  ☑ 功能3    │
│  │  内容展示区      │  ☑ 功能4    │
│  │                 │  ☑ 功能5    │
│  └─────────────────┘             │
├─────────────────────────────────┤
│  ✅ [底部提示]                    │ ← 底部提示
└─────────────────────────────────┘
```

**适用场景：** P1-任务介绍、P3-需求说明、P10-最终效果展示

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。

页面内容分为2个区域：
1. 浏览器预览窗口：模拟浏览器窗口，地址栏显示 [URL]，窗口内展示[具体内容]，用[风格.线条风格]框组织，配手绘浏览器图标
2. 功能清单：用[风格.功能图标]列出[数量]个功能点（[功能1]、[功能2]...）

[风格.约束模板]
页面内容从上到下均匀分布，不要集中在上半部分。
```

**铅笔手绘风示例：**
```
实战案例：用豆包做一个歌词日历网页！蓝色超大号粗体（48px）标题，字号明显大于正文（18px），位于页面顶部。

页面内容分为2个区域：
1. 浏览器预览窗口：模拟浏览器窗口，地址栏显示 localhost:3000，窗口内展示歌词日历网页效果，用蓝色手绘线条框组织，配手绘浏览器图标
2. 功能清单：用绿色手绘对勾图标列出5个功能点（自动生成歌词、日历样式、多歌曲切换、杨千嬅紫配色、手机自适应）

纯白背景。手绘风格，蓝色墨水笔线条。标题蓝色，内容用蓝色手绘线条框组织，不要实色填充背景。用手绘小图标代替emoji。颜色克制，主要使用蓝色，最多1个绿色点缀。不要画页码、系列名、网格线、徽章。
```

---

## 布局 B：手机双屏对比

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
│  [副标题]                        │ ← 较小字号
├─────────────────────────────────┤
│  ┌──────────┐    ┌──────────┐  │
│  │  左手机屏 │ → │  右手机屏 │  │ ← 手绘箭头
│  │  (输入)   │    │  (输出)   │  │
│  └──────────┘    └──────────┘  │
├─────────────────────────────────┤
│  [底部提示框]                    │ ← 底部提示
└─────────────────────────────────┘
```

**适用场景：** 展示"发送指令→AI生成"流程、"修改前→修改后"对比

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。副标题"[副标题文案]"用[风格.副标题样式]。

页面内容分为左右两个手机屏幕：

左侧手机屏幕：模拟 [App名称] App 界面，顶部状态栏显示时间，标题栏显示"[App名称]"，中间[风格.内容容器]展示[具体内容]

右侧手机屏幕：模拟 [App名称] App 界面，顶部状态栏显示时间，标题栏显示"[App名称]"，中间[风格.内容容器]展示[具体内容]

中间用[风格.连接箭头]从左手机指向右手机。

底部区域：[风格.提示框样式]，框内[风格.图标] + "[提示文字]"，其中"[关键词]"用[风格.高亮样式]标出。

[风格.约束模板]
[风格.点缀模板]
页面内容从上到下均匀分布，不要集中在上半部分。
```

**关键成功要素：**
- ✅ 用"手机屏幕"描述，不用"对话气泡"或"圆角矩形"
- ✅ 每个屏幕包含：状态栏 + 标题栏 + 内容区 + 底部操作栏
- ✅ 中间用"粗手绘曲线箭头"连接，强调"手绘曲线感"
- ✅ 底部提示用风格化提示框，不用固定图标类型
- ✅ 点缀分散在多处（右上角、左侧、右侧、关键词）
- ✅ 副标题文案要口语化

---

## 布局 C：操作步骤时间线（P4-V2 已验证）

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
│  [副标题]                        │ ← 较小字号
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ ① [图标] 步骤1说明          │ ← [风格.普通步骤框]
│  └───────────────────────────┘  │
│         ↕ [风格.连接元素]        │
│  ┌───────────────────────────┐  │
│  │ ② [图标] 步骤2说明          │ ← [风格.普通步骤框]
│  └───────────────────────────┘  │
│         ↕ [风格.连接元素]        │
│  ┌───────────────────────────┐  │
│  │ ③ [图标] 步骤3说明          │ ← [风格.强调步骤框]
│  └───────────────────────────┘  │
│         ↕ [风格.连接元素]        │
│  ┌───────────────────────────┘  │
│  │ ④ [图标] 步骤4说明          │ ← [风格.普通步骤框]
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

**适用场景：** 多步操作教程、跟着做系列

**关键成功要素：**
- ✅ **每个步骤必须用边框容器包裹**，否则内容会显得散乱
- ✅ 内容描述用"框内左侧/框内右侧/框内上方/框内下方"结构
- ✅ 普通步骤用风格主色框，需要强调的关键步骤用风格强调色框
- ✅ 步骤间用风格连接元素连接（上一个框底部指向下一个框顶部）
- ✅ 每个步骤左侧放编号圆圈，右侧放图标+文字

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。下方副标题"[副标题文案]"，[风格.副标题样式]。

页面内容从上到下分为[数量]个区域，每个区域用[风格.边框容器]包裹，框内内容清晰结构化：

第1步（[风格.普通步骤框]包裹）
框内左侧：手绘圆形编号"1"（[风格.编号样式]）
框内右侧：
- 上方：手绘[图标]（[图标描述]）
- 中间：文字"[步骤说明文字]"
- 下方：[可选：补充内容描述]

第2步（[风格.普通步骤框]包裹）
框内左侧：手绘圆形编号"2"（[风格.编号样式]）
框内右侧：
- 手绘[图标]（[图标描述]）
- 文字："[步骤说明文字]"

第N步（[风格.强调步骤框]包裹，重点强调）
框内左侧：手绘圆形编号"N"（[风格.强调编号样式]）
框内右侧：
- 上方：手绘[图标]（[图标描述]）
- 中间：文字"[关键步骤说明]"
- "[关键词]"用[风格.高亮样式]标注
- 下方：[可选：小提示文字]

[数量]个步骤框之间用[风格.连接元素]连接（上一个框底部指向下一个框顶部）。

[风格.点缀模板]
页面内容从上到下均匀分布，不要集中在上半部分。如果内容较少，在底部添加装饰性总结框或提示框，撑满整图。
```

**铅笔手绘风示例：**
```
第3步：重命名文件！蓝色超大号粗体（48px）标题，字号明显大于正文（18px），位于页面顶部。下方副标题"给它一个有意义的名字"，字号较小，蓝色。

页面内容从上到下分为4个区域，每个区域用手绘蓝色圆角矩形框包裹，框内内容清晰结构化：

第1步（手绘蓝色圆角矩形框包裹）
框内左侧：手绘圆形编号"1"（蓝色粗边框）
框内右侧：
- 上方：手绘复制图标（两个重叠的矩形）
- 中间：文字"复制代码到剪贴板"

第2步（手绘蓝色圆角矩形框包裹）
框内左侧：手绘圆形编号"2"（蓝色粗边框）
框内右侧：
- 手绘文件图标（矩形带折角）
- 文字："浏览器新建空白HTML文件"

第3步（手绘红色圆角矩形框包裹，重点强调）
框内左侧：手绘圆形编号"3"（红色粗边框）
框内右侧：
- 上方：手绘浏览器图标（圆角矩形+地址栏）
- 中间：文字"将代码粘贴到文件中并保存"
- "粘贴"用红色粗体标注，旁边配手绘警告三角图标
- 下方：小提示"别忘记保存！"

第4步（手绘蓝色圆角矩形框包裹）
框内左侧：手绘圆形编号"4"（蓝色粗边框）
框内右侧：
- 手绘刷新图标（圆形箭头）
- 文字："浏览器中打开HTML文件查看效果"

4个步骤框之间用蓝色手绘虚线箭头连接（上一个框底部指向下一个框顶部），营造"跟着做"的引导感。

点缀元素（分布在页面各处，不要集中在一个位置）：
- 手绘橙色小星星（2-3个，分散在页面空白处）
- 手绘彩色小彩带（1-2条，橙色或紫色，斜向飘落）
- 手绘小圆圈（3-4个，蓝色或橙色，大小不一，散布在步骤之间）
- 手绘小点（5-6个，蓝色，随机分布在空白区域）

页面内容从上到下均匀分布，不要集中在上半部分。
```

---

## 布局 D：多方案卡片（P6-V3 已验证）

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
│  [副标题]                        │ ← 较小字号
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ ⚠️ [问题描述]               │ ← [风格.问题色块]
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ ✅ 方案A（[风格.推荐卡片]）  │ ← 卡片1
│  │ 1️⃣ 步骤1                    │
│  │ 2️⃣ 步骤2                    │
│  │ 3️⃣ 步骤3                    │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 💬 方案B（[风格.方案卡片]）  │ ← 卡片2
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ 📋 方案C（[风格.方案卡片]）  │ ← 卡片3
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

**适用场景：** 多方案选择页、问题解决思路、调试步骤

**关键成功要素：**
- ✅ **卡片竖向排列**，每个卡片用不同颜色边框区分
- ✅ 推荐方案用风格推荐色边框 + 右上角"推荐"小标签
- ✅ 每个方案卡片顶部放对应图标
- ✅ 方案A如果包含多步骤，用 emoji 序号引导阅读
- ✅ 如果方案内已包含完整示例，不需要额外底部代码块模块
- ✅ 问题描述区域用风格问题色块包裹

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。下方副标题"[副标题文案]"，[风格.副标题样式]。

页面内容从上到下分为2个区域：

【问题描述区域】（占据页面顶部约25%高度）
用[风格.问题色块]包裹，色块内：
- 左侧手绘[风格.警告图标]
- 右侧文字："[问题描述内容]"
- "[关键词]"用[风格.高亮样式]标出

【多种解决方案】（占据页面中下部约70%高度）
三个[风格.卡片容器]竖向排列，每个卡片用不同颜色边框：

方案A卡片（[风格.推荐卡片样式]，标注"推荐"）：
- 顶部手绘[图标] + "[方案A名称]"
- 内容："[方案A详细说明，可包含多步骤]"
- 右上角手绘[风格.推荐标签]

方案B卡片（[风格.方案卡片样式]）：
- 顶部手绘[图标] + "[方案B名称]"
- 内容："[方案B详细说明]"

方案C卡片（[风格.方案卡片样式]）：
- 顶部手绘[图标] + "[方案C名称]"
- 内容："[方案C详细说明]"

[风格.点缀模板]
页面内容从上到下均匀分布，不要集中在上半部分。
```

---

## 布局 E：问题调试页（P7-V4 已验证）

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
├─────────────────────────────────┤
│  ┌────┐  ┌────┐  ┌────┐       │
│  │Day1│  │Day2│  │Day3│       │ ← 问题场景展示
│  └────┘  └────┘  └────┘       │
│  ← [风格.标注] →                │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ ✅ 方法1（[风格.推荐卡片]）  │ ← 卡片1（内嵌代码）
│  └───────────────────────────┘  │
│  ┌───────────────────────────┘  │
│  │ 💻 方法2（[风格.方案卡片]）   │ ← 卡片2（内嵌代码）
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

**适用场景：** 问题调试页、错误修复指导、优化建议

**关键成功要素：**
- ✅ **顶部问题场景**：用日历格/对比图等方式直观展示问题，不要加区域标题文字
- ✅ **风格化标注**：用风格强调色 + 箭头 + 文字标注问题位置
- ✅ **方法卡片竖向排列**：每个方法卡片内可嵌入代码块
- ✅ **去掉底部单独模块**：如果方法内已包含完整示例，不需要额外底部模块
- ✅ **2个区域即可**：顶部问题场景（30%）+ 中部方法卡片（65%）

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。

页面内容从上到下分为2个区域：

页面顶部（约30%高度）：
[问题场景描述，如"模拟3个手机屏幕样式的日历格，横向排列"]
[具体问题标注："用[风格.标注样式]圈出XX，旁边标注[风格.标注文字]'XX问题！'，用箭头从标注指向XX"]

页面中部（约65%高度）：
两个[风格.卡片容器]竖向排列：

方法1卡片（[风格.推荐卡片样式]，标注"推荐"）：
- 顶部手绘[图标] + "[方法1名称]"
- 内容："[方法1说明]"
- 下方用[风格.代码块样式]展示[代码内容]
- 右上角手绘[风格.推荐标签]

方法2卡片（[风格.方案卡片样式]）：
- 顶部手绘[图标] + "[方法2名称]"
- 内容："[方法2说明]"
- 下方用[风格.代码块样式]展示关键代码
- 代码中"[关键词]"用[风格.代码高亮样式]标出

[风格.点缀模板]
页面内容从上到下均匀分布，不要集中在上半部分。
```

---

## 布局 F：多宫格展示（P8-V5 已验证）

```
┌─────────────────────────────────┐
│  [标题]                          │ ← 超大标题
│  [副标题]                        │ ← 较小字号
├─────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐    │
│  │  手机屏1  │  │  手机屏2  │    │ ← 2x2 网格
│  └──────────┘  └──────────┘    │
│  ┌──────────┐  ┌──────────┘    │
│  │  手机屏3  │  │  手机屏4  │    │
│  └──────────┘  └──────────┘    │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ ☑ 功能点1                  │ ← 功能清单长条框
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ ☑ 功能点2                  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │ ☑ 功能点3                  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┘  │
│  │ ☑ 功能点4                  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

**适用场景：** 最终效果展示、多版本对比、功能清单展示

**关键成功要素：**
- ✅ **手机屏幕下方截断**：只展示到歌词/核心内容区域即可，不需要完整手机底部
- ✅ **手机屏幕撑满左右**：2x2 网格中每个手机屏幕宽度要撑满画布左右两侧，不要左右留白
- ✅ **手机内预览一致性**：每个手机内的网页预览效果与之前页面的预览保持一致（配色、布局、样式）
- ✅ **底部功能清单长条框**：每个功能点用圆角矩形长条框包裹，左侧 checkbox 对勾，右侧文字
- ✅ **长条框竖向排列**：每个长条框横向撑满页面宽度，从上到下依次排列（不要并排成多列）
- ✅ **多状态展示**：如需展示不同状态（如不同 tab 选中），确保每个手机屏幕的状态标注清晰

**Prompt 模板（风格无关结构）：**
```
[标题]！[风格.标题样式]，位于页面顶部。下方副标题"[副标题文案]"，[风格.副标题样式]。

页面内容从上到下分为3个区域：

页面顶部（约5%高度）：标题和副标题区域。

页面中部（约60%高度）：2x2 网格排列的4个手机屏幕预览，手机屏幕撑满画布左右两侧，左右基本无留白：

左上手机屏幕（[状态1描述]）：
模拟手机屏幕，下方截断，只展示到核心内容区域即可。屏幕内：
- [内容描述，如"风格标签、歌词卡片、翻页按钮等"]
- [当前状态，如"某某标签高亮选中"]

右上手机屏幕（[状态2描述]）：
模拟手机屏幕，下方截断。布局同上。
- [内容描述]
- [当前状态]

左下手机屏幕（[状态3描述]）：
模拟手机屏幕，下方截断。布局同上。
- [内容描述]
- [当前状态]

右下手机屏幕（[状态4描述]）：
模拟手机屏幕，下方截断。布局同上。
- [内容描述]
- [当前状态]

[多屏幕整体配色说明]

页面底部（约35%高度）：[数量]个手绘圆角矩形长条框从上到下依次竖向排列（每个长条框横向撑满页面宽度，不要并排成多列），每个框内左侧手绘checkbox对勾图标（[风格.功能色]），右侧文字：
- 第一个长条框：手绘[风格.功能色]对勾 + "[功能点1]"
- 第二个长条框：手绘[风格.功能色]对勾 + "[功能点2]"
- 第三个长条框：手绘[风格.功能色]对勾 + "[功能点3]"
- 第四个长条框：手绘[风格.功能色]对勾 + "[功能点4]"

[风格.点缀模板]
页面内容从上到下均匀分布，不要集中在上半部分。
```
